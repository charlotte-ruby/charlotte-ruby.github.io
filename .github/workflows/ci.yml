name: Run Rubocop

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["*"]

jobs:
  rubocop:
    runs-on: ubuntu-latest
    steps:
      - name: Rubocop checks
        uses: luizfonseca/github-actions-rubocop@1.5.6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
